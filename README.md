# webserv
HTTP/1.1 server in C++ from scratch. Non-blocking I/O with select, Nginx-style config (virtual hosts, locations, redirects), static files, file uploads, CGI (Python/PHP). GET, POST, DELETE. No external libs — just sockets and the spec.
